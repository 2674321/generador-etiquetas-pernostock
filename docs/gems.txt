require 'prawn' (PDF)
require 'barby' (generador codigo de barras)
require 'barby/barcode/code_128' (solo se usa su #encoding: los módulos se dibujan
                                  manualmente, compartidos entre PDF y vista previa)
require 'roo' (lector de xlsx/xlsm/ods)
require 'roo-xls' (lector de xls)
require 'fileutils' (creacion de directorios de salida)
require 'gtk3' (interfaz grafica)
require 'cairo' (vista previa de la etiqueta)
require 'tty-progressbar' (variantes historicas / ya no se usa en el nucleo)
require 'date' (fecha tiempo real, historico)
require 'sqlite3' (Base de Datos, historico)

(Devkit y gems) Instalar como AD
CMD como AD


gem install prawn
gem install roo-xls
gem install roo
gem install barby
gem install gtk3
gem install cairo
gem install tty-progressbar
gem install date
gem install sqlite3
